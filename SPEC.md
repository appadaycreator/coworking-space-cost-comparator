# コワーキングスペース費用比較 - SPEC

## Version History

### v2.2 (2026-09-08)
**Phase 2 直接実装 - M1(アフィリエイトCTA)、M10(印刷・エクスポート)**

#### 実装内容
- **M1**: 診断結果直後にアフィリエイトCTA（コワーキングスペース検索リンク - A8.net px.a8.net）を自然な流れで配置。ユーザーが価値を感じた瞬間のアクション喚起
- **M10**: 印刷・エクスポート機能
  - 🖨️ 印刷ボタン（window.print()）
  - 📋 結果をクリップボードにコピー
  - 📥 CSV ダウンロード機能
- 印刷時用の @media print CSS 追加（広告・チャート非表示）

### v2.1 (2026-09-08)
**Phase 2 改善実装 - M1, M3, M4 + 5つの視覚的改善**

#### 実装内容
- **M1**: 計算結果画面に「この結果を保存」CTA ボタン追加 (緑色・高視認性)
- **M3**: localStorage を使用した計算履歴の自動保存・復元機能
  - 最大10件までの履歴をブラウザに保存
  - 履歴から前回データをワンクリック復元
  - 履歴クリア機能
- **M4**: Chart.js によるコスト内訳の可視化（ドーナツチャート）
  - 月額利用料と交通費の比率を視覚的に表示
  - レスポンシブ対応・ツールチップ付き

#### 視覚的改善（V1-V5）
- **V1**: ボタン可視性向上
  - 「📊 費用を比較して診断する」に統一
  - グラデーション背景・フォントサイズ拡大
  - ボタン下に「登録不要・無料」テキスト追加

- **V2**: 関連サービスリンク の目立たせ化
  - 小さな青テキストリンク → ボタンスタイルカード
  - `service-link-btn` クラスで統一スタイリング
  - ホバーエフェクト追加

- **V3**: ヘッダー〜フォーム間の余白削減
  - header padding 32px → 24px
  - container margin-top -8px で詰め込み
  - 全体的なデザイン密度向上

- **V4**: フォームフィールドへの説明文追加
  - 各ラベルに `label-hint` クラスで補足情報を表示
  - 「月額利用料」「月間利用日数」などの意味を明確化

- **V5**: モバイル表示の最適化
  - 入力フォームのフォントサイズ 16px に統一（タッチ時の自動ズーム防止）
  - padding 調整で全体の収まりを改善
  - canvas 要素の max-width: 100% 指定

#### 技術詳細
- Chart.js v4.4.0 CDN リンク追加
- localStorage キー: `cwkHistory` (JSON配列)
- 計算後、結果オブジェクトは `currentResult` グローバル変数に保存
- `drawChart()` / `saveToHistory()` / `renderHistory()` 関数実装
- 計算結果の表示後、自動的に結果セクションへスクロール

#### 構造化データ対応状況
- FAQPage schema: ✓ 実装済み (5問)
- HowTo schema: ✓ 実装済み (5ステップ)
- WebApplication schema: ✓ 実装済み
- Organization schema: 未実装（次回対応予定）

#### SEO対応状況
- メタタイトル: ✓ キーワード含有
- メタディスクリプション: ✓ キーワード含有
- OGP / Twitter Card: ✓ 実装済み
- Canonical: ✓ 実装済み
- GSC インデックス状況: 要改善 (表示回数 0 件)

---

### v1.0 (2026-05-14)
- 初版リリース
- 基本計算機能実装
- 診断ロジック実装
